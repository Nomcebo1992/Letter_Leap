import React from 'react';
import ReactDOM from 'react-dom';
import LetterLeap from './LetterLeap';

ReactDOM.render(
  <React.StrictMode>
    <LetterLeap />
  </React.StrictMode>,
  document.getElementById('root')
);
